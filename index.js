import { before, describe, it, test } from "node:test";

after(() => { });
afterEach(() => { });

before(() => { });
beforeEach(() => { });

describe("", () => { })

describe.only("", () => { })

describe.skip("", () => { })

describe.todo("", () => { })

it("", () => { });

it.only("", () => { });

it.skip("", () => { });

it.todo("", () => { });

test("", () => { });

test.only("", () => { });

test.skip("", () => { });

test.todo("", () => { });

